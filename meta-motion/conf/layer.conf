# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-motion"
BBFILE_PATTERN_meta-motion = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-motion = "6"

LAYERDEPENDS_meta-motion = "core"
LAYERSERIES_COMPAT_meta-motion = "kirkstone"
