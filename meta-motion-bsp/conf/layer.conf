# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-motion-bsp"
BBFILE_PATTERN_meta-motion-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-motion-bsp = "6"

LAYERDEPENDS_meta-motion-bsp = "core"
LAYERSERIES_COMPAT_meta-motion-bsp = "kirkstone"
