# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-motion-distro"
BBFILE_PATTERN_meta-motion-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-motion-distro = "6"

LAYERDEPENDS_meta-motion-distro = "core"
LAYERSERIES_COMPAT_meta-motion-distro = "kirkstone"
